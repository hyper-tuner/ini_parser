const namePattern = r'\s*{[^{}]+}\s*';
const textPattern = '[^,={}]+';
const textPatternNoSpace = '[^,={} ]+';
const expPattern = '{[^{}]+}';
const bitsPattern = r'\[.+:.+\]+';
const bitsListPattern = r'\s*(\d+="[\w, -]+")+';
